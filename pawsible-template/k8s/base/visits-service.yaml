apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
  namespace: pawsible
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
    spec:
      containers:
      - name: visits-service
        image: 268015775379.dkr.ecr.us-east-1.amazonaws.com/pawsible/visits-service:1.0.0
        ports:
        - containerPort: 8082
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: visits-service
  namespace: pawsible
spec:
  selector:
    app: visits-service
  ports:
  - port: 8082
    targetPort: 8082
